import 'package:flutter/material.dart';

class HeartARScreen extends StatelessWidget {
  const HeartARScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Heart AR Screen')));
  }
}
